<div class="flex-1 overflow-y-auto">
  <div class="max-w-3xl mx-auto p-4 sm:p-8">
    <div class="mb-8">
      <%= link_to team_recurring_issues_path(@team), class: "text-xs text-gray-500 hover:text-gray-300 cursor-pointer" do %>
        &larr; Recurring issues
      <% end %>
      <h1 class="text-2xl font-semibold text-gray-100 mt-2"><%= title %></h1>
    </div>
    <%= yield %>
  </div>
</div>
