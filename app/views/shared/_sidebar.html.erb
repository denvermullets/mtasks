<aside id="sidebar" class="w-56 bg-menu border-r border-stroke flex-col hidden sm:flex">
  <!-- User -->
  <div class="h-14 px-4 border-b border-stroke flex items-center">
    <div class="flex items-center justify-between w-full">
      <%= render "shared/avatar_color_picker" %>
      <button type="button"
              class="relative p-1.5 text-gray-400 hover:text-gray-200 rounded-md hover:bg-gray-800 cursor-pointer"
              data-action="click->notification-drawer#toggle"
              aria-label="Notifications">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
        </svg>
        <% unread_count = current_user.notifications.unread.count %>
        <%= render "notifications/badge", count: unread_count %>
      </button>
    </div>
  </div>
  <!-- Navigation -->
  <div class="flex-1 overflow-y-auto">
    <% if current_team.present? %>
      <div class="p-2">
        <%= link_to team_issues_path(current_team, assignee_ids: current_user.id), class: "flex items-center gap-2 px-2 py-1.5 text-sm rounded hover:bg-foreground text-gray-300 cursor-pointer" do %>
          <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
          </svg>
          <span>My issues</span>
        <% end %>
      </div>
    <% end %>
    <% owned_teams, joined_teams = user_teams.partition { |t| team_owner?(t) } %>

    <div class="p-2 mt-1" data-controller="teams-section">
      <!-- Header with toggle and + button -->
      <div class="flex items-center justify-between px-2 mb-2">
        <button data-action="click->teams-section#toggle"
                class="flex items-center gap-1 text-xs text-gray-500 flex-1 text-left hover:text-gray-400 cursor-pointer">
          <svg data-teams-section-target="chevron"
               class="w-3 h-3 transition-transform"
               fill="currentColor"
               viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"></path>
          </svg>
          <span>Teams you own</span>
        </button>
        <%= link_to new_team_path, class: "p-1 text-gray-500 hover:text-gray-300" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
        <% end %>
      </div>

      <div class="space-y-1" data-teams-section-target="list">
        <% if owned_teams.any? %>
          <% owned_teams.each do |team| %>
            <%= render "shared/sidebar_team_row", team: team %>
          <% end %>
        <% else %>
          <p class="px-2 py-1.5 text-xs text-gray-500">No teams yet. Create one above.</p>
        <% end %>
      </div>
    </div>

    <% if joined_teams.any? %>
      <div class="p-2 mt-2" data-controller="teams-section">
        <div class="flex items-center px-2 mb-2">
          <button data-action="click->teams-section#toggle"
                  class="flex items-center gap-1 text-xs text-gray-500 flex-1 text-left hover:text-gray-400 cursor-pointer">
            <svg data-teams-section-target="chevron"
                 class="w-3 h-3 transition-transform"
                 fill="currentColor"
                 viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"></path>
            </svg>
            <span>Teams you've joined</span>
          </button>
        </div>
        <div class="space-y-1" data-teams-section-target="list">
          <% joined_teams.each do |team| %>
            <%= render "shared/sidebar_team_row", team: team %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
  <!-- Actions -->
  <div class="p-4 border-t border-stroke space-y-3">
    <% if current_team.present? %>
      <%= link_to new_import_path, class: "text-xs text-gray-400 hover:text-gray-300 block" do %>
        Import CSV
      <% end %>
    <% end %>

    <%= link_to changelog_path, class: "text-xs text-gray-400 hover:text-gray-300 block" do %>
      What's new
    <% end %>

    <% if current_team.present? %>
      <div class="pt-3">
        <button type="button"
                data-action="click->keyboard-shortcuts#showModal"
                class="text-xs text-gray-500 hover:text-gray-400 flex items-center gap-1.5">
          <span>Shortcuts</span>
          <kbd class="px-1.5 py-0.5 text-xs font-semibold text-gray-500 bg-background border border-stroke rounded">
            <span class="text-gray-600">⌘</span> K
          </kbd>
        </button>
      </div>
    <% end %>
  </div>
</aside>
