<div class="flex-1 flex flex-col bg-background">
  <!-- Top Bar -->
  <div class="h-14 bg-menu border-b border-stroke flex items-center justify-between px-4">
    <div class="flex items-center gap-4">
      <%= render "shared/mobile_menu_button" %>
      <%= link_to team_issues_path(current_team),
          class: "text-sm text-gray-500 hover:text-gray-400 flex items-center gap-2" do %>
        <span>&#8592;</span>
        <span class="hidden sm:inline"><%= current_team.name %></span>
        <span class="sm:hidden">Back</span>
      <% end %>
      <span class="text-gray-600 hidden sm:inline">&#8250;</span>
      <span class="text-sm text-gray-400 hidden sm:inline">New issue</span>
    </div>
  </div>
  <!-- Form Content -->
  <div class="flex-1 overflow-y-auto p-8">
    <div class="max-w-4xl mx-auto">
      <%= form_with model: [@team, @issue], data: { controller: "issue-form" } do |form| %>
        <!-- Team Display (Read-only) -->
        <div class="mb-6 flex items-center gap-2">
          <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm font-medium">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path>
            </svg>
            <%= current_team.identifier %>
            <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path>
            </svg>
          </div>
          <span class="text-gray-600">&#8250;</span>
          <span class="text-gray-400 text-sm">New issue</span>
        </div>
        <!-- Issue Title -->
        <div class="mb-6">
          <%= form.text_field :title,
              placeholder: "Issue title",
              autofocus: true,
              required: true,
              autocomplete: "off",
              class: "w-full bg-transparent border-0 text-gray-200 text-2xl font-semibold placeholder-gray-600 focus:outline-none focus:ring-0 p-0 #{'border-b-2 border-red-500' if @issue.errors[:title].any?}" %>
          <% if @issue.errors[:title].any? %>
            <div class="text-red-400 text-sm mt-2">
              <%= @issue.errors[:title].first %>
            </div>
          <% end %>
        </div>
        <!-- Description -->
        <div class="mb-6">
          <%= form.text_area :description,
              placeholder: "Add description...",
              rows: 4,
              autocomplete: "off",
              class: "w-full bg-transparent border-0 text-gray-300 placeholder-gray-600 focus:outline-none focus:ring-0 p-0 resize-none" %>
        </div>
        <!-- Property Buttons -->
        <div class="flex flex-wrap gap-2 mb-8">
          <!-- Lane (Backlog) -->
          <% selected_lane = @issue.lane_id ? @lanes.find { |l| l.id == @issue.lane_id } : @lanes.first %>
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleDropdown"
              data-dropdown="lane"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
              </svg>
              <span data-issue-form-target="laneLabel">
                <%= selected_lane&.name || "Backlog" %>
              </span>
            </button>
            <div data-issue-form-target="laneDropdown" class="hidden absolute top-full left-0 mt-1 min-w-[200px] bg-foreground border border-stroke rounded-md shadow-lg z-10">
              <% @lanes.each do |lane| %>
                <button type="button"
                        data-action="click->issue-form#selectLane"
                  data-lane-id="<%= lane.id %>"
                  data-lane-name="<%= lane.name %>"
                  class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors first:rounded-t-md last:rounded-b-md">
                  <%= lane.name %>
                </button>
              <% end %>
            </div>
            <%= form.hidden_field :lane_id, value: selected_lane&.id, data: { issue_form_target: "laneInput" } %>
          </div>
          <!-- Priority -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleDropdown"
              data-dropdown="priority"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6h18M3 12h18M3 18h18"></path>
              </svg>
              <span data-issue-form-target="priorityLabel">No priority</span>
            </button>
            <div data-issue-form-target="priorityDropdown" class="hidden absolute top-full left-0 mt-1 min-w-[200px] bg-foreground border border-stroke rounded-md shadow-lg z-10">
              <% [["No priority", "no_priority"], ["Urgent", "urgent"], ["High", "high"], ["Medium", "medium"], ["Low", "low"]].each do |label, value| %>
                <button type="button"
                        data-action="click->issue-form#selectPriority"
                  data-priority-value="<%= value %>"
                  data-priority-label="<%= label %>"
                  class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors first:rounded-t-md last:rounded-b-md">
                  <%= label %>
                </button>
              <% end %>
            </div>
            <%= form.hidden_field :priority, value: "no_priority", data: { issue_form_target: "priorityInput" } %>
          </div>
          <!-- Assignee -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleDropdown"
              data-dropdown="assignee"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
              <span data-issue-form-target="assigneeLabel">No assignee</span>
            </button>
            <div data-issue-form-target="assigneeDropdown" class="hidden absolute top-full left-0 mt-1 min-w-[200px] bg-foreground border border-stroke rounded-md shadow-lg z-10">
              <button type="button"
                      data-action="click->issue-form#selectAssignee"
                data-assignee-id=""
                data-assignee-name="No assignee"
                class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors first:rounded-t-md">
                No assignee
              </button>
              <% @team_members.each do |member| %>
                <button type="button"
                        data-action="click->issue-form#selectAssignee"
                  data-assignee-id="<%= member.id %>"
                  data-assignee-name="<%= member.name %>"
                  class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors last:rounded-b-md">
                  <%= member.name %>
                </button>
              <% end %>
            </div>
            <%= form.hidden_field :assignee_id, value: "", data: { issue_form_target: "assigneeInput" } %>
          </div>
          <!-- Project -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleDropdown"
              data-dropdown="project"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
              </svg>
              <span data-issue-form-target="projectLabel">No project</span>
            </button>
            <div data-issue-form-target="projectDropdown" class="hidden absolute top-full left-0 mt-1 min-w-[200px] bg-foreground border border-stroke rounded-md shadow-lg z-10">
              <button type="button"
                      data-action="click->issue-form#selectProject"
                data-project-id=""
                data-project-name="No project"
                class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors first:rounded-t-md">
                No project
              </button>
              <% @projects.each do |project| %>
                <button type="button"
                        data-action="click->issue-form#selectProject"
                  data-project-id="<%= project.id %>"
                  data-project-name="<%= project.name %>"
                  class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors last:rounded-b-md">
                  <%= project.name %>
                </button>
              <% end %>
            </div>
            <%= form.hidden_field :project_id, value: "", data: { issue_form_target: "projectInput" } %>
          </div>
          <!-- Estimate -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleDropdown"
              data-dropdown="estimate"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              <span data-issue-form-target="estimateLabel">No estimate</span>
            </button>
            <div data-issue-form-target="estimateDropdown" class="hidden absolute top-full left-0 mt-1 min-w-[200px] bg-foreground border border-stroke rounded-md shadow-lg z-10">
              <button type="button"
                      data-action="click->issue-form#selectEstimate"
                data-estimate-value=""
                data-estimate-label="No estimate"
                class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors first:rounded-t-md">
                No estimate
              </button>
              <% [1, 2, 3, 5, 8, 13].each do |points| %>
                <button type="button"
                        data-action="click->issue-form#selectEstimate"
                  data-estimate-value="<%= points %>"
                  data-estimate-label="<%= points %> <%= 'point'.pluralize(points) %>"
                  class="w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-hover-highlight transition-colors last:rounded-b-md">
                  <%= points %> <%= 'point'.pluralize(points) %>
                </button>
              <% end %>
            </div>
            <%= form.hidden_field :estimate, value: "", data: { issue_form_target: "estimateInput" } %>
          </div>
          <!-- Labels -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleLabelsDropdown"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
              </svg>
              <span data-issue-form-target="labelsLabel">Labels</span>
            </button>
            <%= render "labels/new_issue_picker", team_id: @team.id, labels: @labels %>
          </div>
          <!-- Milestone -->
          <div class="relative">
            <button type="button"
                    data-action="click->issue-form#toggleMilestonesDropdown"
              class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-foreground border border-stroke text-gray-300 text-sm hover:bg-hover-highlight transition-colors">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
              </svg>
              <span data-issue-form-target="milestoneLabel">No milestone</span>
            </button>
            <%= render "milestones/new_issue_picker", team_id: @team.id %>
          </div>
        </div>
        <!-- Bottom Actions -->
        <div class="flex items-center justify-between pt-6 border-t border-stroke">
          <div class="flex items-center gap-4">
            <!-- Attachment button -->
            <button type="button" class="text-gray-500 hover:text-gray-400">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path>
              </svg>
            </button>
          </div>
          <div class="flex items-center gap-4">
            <!-- Create more toggle -->
            <label class="flex items-center gap-2 text-sm text-gray-400 cursor-pointer">
              <input type="checkbox" name="create_more" value="1" class="rounded bg-background border-stroke accent-accent  focus:ring-offset-0">
              <span>Create more</span>
            </label>
            <!-- Submit button -->
            <%= form.submit "Create issue",
                class: "px-4 py-2 rounded-md border-accent border hover:bg-accent hover:text-gray-800 text-white font-medium cursor-pointer transition-colors" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
